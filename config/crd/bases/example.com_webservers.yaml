apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: webservers.example.com
spec:
  group: example.com
  scope: Namespaced
  names:
    kind: WebServer
    listKind: WebServerList
    plural: webservers
    singular: webserver
    shortNames:
      - ws
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                type:
                  type: string
                  enum:
                    - nginx
                    - httpd
                  title: "Web Server Type"
                  description: "Select which web server to deploy (nginx or httpd)."
                  x-descriptors:
                    - "urn:alm:descriptor:com.tectonic.ui:select:nginx"
                    - "urn:alm:descriptor:com.tectonic.ui:select:httpd"
                replicas:
                  type: integer
                  format: int32
                  minimum: 1
                  default: 1
                  title: "Replicas"
                  description: "Number of pod replicas."
                  x-descriptors:
                    - "urn:alm:descriptor:com.tectonic.ui:number"
                port:
                  type: integer
                  format: int32
                  default: 8080
                  title: "Container Port"
                  description: "Port on which the web server listens inside the container."
                  x-descriptors:
                    - "urn:alm:descriptor:com.tectonic.ui:text"
                image:
                  type: string
                  title: "Custom Image"
                  description: >-
                    Optional override container image. If left empty, a default
                    image will be used:
                    nginx -> docker.io/library/nginx:1.27-alpine,
                    httpd -> docker.io/library/httpd:2.4-alpine.
                  x-descriptors:
                    - "urn:alm:descriptor:com.tectonic.ui:text"
              required:
                - type
            status:
              type: object
              properties:
                phase:
                  type: string
                message:
                  type: string
      subresources:
        status: {}
